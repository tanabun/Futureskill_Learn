function plus(x, y) {
  return x + y;
}
var r = plus(10, 20);
console.log("result:", r);

function log(data) {
  console.log(data);
  return;
}
function plus2(x, y) {
  return plus(x, y);
}
function area(w, h, t) {
  if (t == "square") return w * h;
  else return Math.PI * x * x;
}
log(plus2(3, 4));
log(area(10, 10, "square"));
log(area(10));
